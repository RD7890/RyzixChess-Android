*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx

# Signing key is committed permanently — same key across all builds (no reinstall needed)
# signing/signing-key.jks  <- intentionally NOT ignored

# Stockfish binary (compiled in CI automatically)
app/src/main/assets/stockfish

# Build outputs
app/build/
